---
// Deliberately resembles a violation: this is build-time source, not user-facing sample code.
import { useTool } from 'annota';
---

<Code
  code={`import { useTool } from 'annota/react';`}
  lang="tsx"
/>
